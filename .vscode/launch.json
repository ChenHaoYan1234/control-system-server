{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run server",
            "request": "launch",
            "command": "yarn run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}
